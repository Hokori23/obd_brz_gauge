# `board_ws_175_amoled` 模块说明

这是 WS175 AMOLED 板级实现。
核心职责：板级初始化、显示与触摸接线收口、共享总线协调。

## 模块负责什么

- 初始化 WS175 对应的显示、触摸和背光链路
- 收口该板型专属的引脚、时序和设备实例
- 向上层暴露统一 `board_*` 能力，而不是泄漏板型细节

## 维护注意点

- 任何只属于 WS175 的显示时序或触摸映射，都应留在这个文件
- 如果多个板型都需要同一能力，优先上提到 `board_dispatch` 或公共驱动
- 上层 UI 和业务层不要直接依赖这里的私有实现
