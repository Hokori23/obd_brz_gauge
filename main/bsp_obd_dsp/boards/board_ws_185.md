# `board_ws_185` 模块说明

这是 WS185 板级实现。
核心职责：板级初始化、显示链路适配、板型差异收口。

## 模块负责什么

- 初始化 WS185 对应的 LCD、触摸和背光资源
- 处理 WS185 与其他板型不同的硬件参数和接线方式
- 通过统一 `board_*` 接口向上层提供能力

## 维护注意点

- 板型专属的时序、旋转、映射关系应继续留在这里
- 如果后续引入新板型，尽量保持上层接口不变，只新增板型实现
- 调整板型行为时，优先验证是否会影响 `board_dispatch` 的统一出口
