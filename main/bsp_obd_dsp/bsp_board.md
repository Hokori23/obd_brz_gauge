# `bsp_board.h` 阅读说明

这个文件很小，但角色很明确：集中定义板级硬件常量。

## 当前主要内容

- LCD 背光引脚
- 背光高低电平定义

## 为什么要有这个文件

这样做的目的不是为了抽象优雅，而是为了把“板子相关的硬件常量”集中放在一个入口。

如果以后换板子，通常先从这里和 LCD/触摸驱动一起改。

## 你可以怎么理解

它相当于：

- 前端里的 `config.ts`
- 后端里的 `env + constants`

只是这里存的是 GPIO 和电平定义。
